# === NỘI BỘ — KHÔNG PUSH ===
# Bộ não trợ lý: trí nhớ, ràng buộc, nhiệm vụ, ghi chép.
BRAIN/
# Draft tin nhắn gửi team (chưa duyệt/chưa gửi).
_team-messages/

# === Token / secrets ===
.env
.env.*
!.env.example
!.env.*.example
secrets.plist
**/secrets.plist
android/app/secrets.properties
ios/**/APISecrets.swift

# === OSX ===
.DS_Store

# === Xcode ===
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
**/.xcode.env.local

# === Android / IntelliJ ===
.idea
.gradle
local.properties
*.iml
*.hprof
.cxx/
*.keystore
!debug.keystore
.kotlin/
android/app/keySigning.bin
android/app/*.keystore
!android/app/debug.keystore
android/app/*.jks
android/app/*.p12
android/gradle.properties

# === node.js ===
node_modules/
npm-debug.log
yarn-error.log

# === CocoaPods / Ruby ===
**/Pods/
/vendor/bundle/

# === Metro ===
.metro-health-check*

# === Testing ===
/coverage

# === Bundle artifacts ===
*.jsbundle

# === Logs / tmp ===
*.log
replay_pid*

# === Yarn ===
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# === Misc tooling ===
.serena
.mcp.json

# === Agent state (nội bộ, KHÔNG public) ===
# `_Agents/` đã chặn từ trước. Phần dưới bổ sung 2026-08-14, sửa lại 15/08.
#
# `CLAUDE.md` CỐ Ý KHÔNG chặn. Bản vá đầu có chặn, và đó là sai hướng: `CLAUDE.md`
# ở gốc kho là tài liệu ranh giới của dự án, và chính nó ghi rằng bản trước biến
# mất 07–08/08/2026 vì KHÔNG CÓ BẢN TRONG GIT. Chặn nó khỏi git là lặp lại đúng cơ
# chế đã làm mất nó, chỉ khác là lần này vĩnh viễn. Giá đã trả là MẤT tệp, không
# phải LỘ tệp. Thêm nữa mẫu trần `CLAUDE.md` khớp mọi cấp thư mục, nên
# `src/modules/*/CLAUDE.md` cũng chết theo.
#
# `.claude/` thì chặn CẢ thư mục chứ không chỉ `settings.local.json`: nó còn chứa
# `worktrees/` — mỗi worktree là một bản chép đầy đủ của kho (đo 14/08: 3 bản,
# 4.023 tệp, 146 MB). Không tệp nào dưới `.claude/` đang được track
# (`git ls-files .claude/` rỗng), nên chặn cả thư mục không làm mất gì.
#
# `CLAUDE.local.md` là tên quy ước sẵn cho ghi chú RIÊNG của từng máy — đúng thứ
# cần chặn. Mẫu cũ không bắt nó. `.claude.json` cũng vậy: nó là tệp, không nằm
# trong thư mục `.claude/`, nên dòng trên không phủ.
_Agents/
.claude/
CLAUDE.local.md
.claude.json


# Expo
.expo
dist/
web-build/

# Nhật ký của scripts/build-aab.ps1 — sinh ra ở gốc kho mỗi lượt dựng.
build-aab.log

# Lõi Rust do `cargo ndk` dựng trước mỗi lượt gradle. SẢN PHẨM DỰNG, không phải
# nguồn: hàng chục MB, và khác nhau theo bản NDK của từng máy. Đưa vào git là biến
# bản dựng của một máy thành thứ mọi người khác dùng lại mà không biết.
android/app/src/main/jniLibs/
