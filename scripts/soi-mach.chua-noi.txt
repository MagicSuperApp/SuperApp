# Nợ mạch dọc — hàm Rust extern "C" chưa có đường gọi nào từ app.
# Sinh lần đầu 2026-08-18 từ origin/develop 3056c48. Gốc: commit 5ab0635 (25/06/2026)
# đổ 46 hàm Rust vào repo trong khi cầu chỉ có 3 — không cổng nào đỏ suốt gần 2 tháng.
#
# Nối được hàm nào thì XOÁ dòng đó. Thêm dòng mới = phải nói lý do trong commit.

taad_build_cancel_recovery_tx
taad_build_create_child_taad_utxo_tx
taad_build_create_taad_utxo_tx
taad_build_deactivate_taad_tx
taad_build_deploy_mint_registry
taad_build_finalize_recovery_tx
taad_build_genesis_supply_state
taad_build_init_recovery_tx
taad_build_multi_pool_delegation_tx
taad_build_rotate_taad_tx
taad_build_signed_transfer
taad_build_stake_delegation_tx
taad_build_stake_deregistration_tx
taad_build_update_guardians_tx
taad_build_update_mint_registry
taad_build_vote_delegation_tx
taad_build_withdraw_reward_tx

# KHÔNG phải nợ — dòng dưới đây cố ý không bao giờ có cầu.
# `taad_clear_last_error` xoá ô lỗi theo luồng. Hợp đồng của ô lỗi là
# `taad_last_error` đọc MỘT LẦN rồi tự xoá, nên tầng app không có việc gì
# phải gọi hàm xoá — mở một cầu tới nó là mở đúng một đường để vứt đi lời
# giải thích mà tầng app sắp đọc. Nơi gọi duy nhất là bài kiểm Rust
# (`rust/taad_enclave_core/src/ffi_last_error_tests.rs`), đặt lại ô giữa hai ca.
taad_clear_last_error

taad_construct_did
taad_derive_cardano_address
taad_derive_cardano_address_account
taad_derive_ed25519_public_key
taad_derive_payment_xprv
taad_derive_payment_xprv_account
taad_enclave_core_free_string
taad_enclave_core_sha256_hex
taad_enclave_core_verify_p256_signature
taad_generate_controller_keypair
taad_hkdf_derive
taad_lampnet_build_recover_request
taad_lampnet_build_upload_request
taad_lampnet_recover_decrypt
taad_publish_did_tx
taad_sha256_bytes_hex
taad_sha256_hex
taad_verify_p256_signature

# Thêm 2026-09-14. Hàm vừa lập cùng ngày, và cổng này nói đúng: TẦNG APP chưa
# gọi được nó ở NỀN NÀO. Đo từng tầng:
#   · Rust C-ABI  : `rust/taad_enclave_core/src/lib.rs:362` — có
#   · JNI         : `src/android_jni.rs:315` `nativeWitnessUnsignedTxEd25519` — có,
#                   nhưng nó gọi hàm Rust nội bộ, KHÔNG gọi bản C-ABI này
#   · Kotlin      : không khai `external fun nativeWitnessUnsignedTxEd25519` — THIẾU
#   · Swift / .m  : không có method nào — THIẾU
#   · TS          : không có — THIẾU
# Nên câu "mở cửa cho cả hai nền" ở commit lập hàm này nói QUÁ: nó đúng ở tầng
# FFI, sai ở tầng gọi được. Ghi nợ thay vì viết cầu ngay, vì chưa màn nào cần
# đường ký này — dựng bốn tầng cầu tới một hàm không ai gọi là dựng thêm một
# đường không ai đi được, đúng thứ vòng rà trước đã tìm ra ở đường ký ví cũ.
# Nối được thì XOÁ dòng dưới.
taad_witness_unsigned_tx_ed25519
