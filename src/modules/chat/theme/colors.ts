// modules/chat/theme/colors.ts
// ProofChat — xanh dương trí tuệ.
//
// Gộp về nguồn token duy nhất (YC-1): brand token nay sống tại theme/tokens.ts.
// File này GIỮ làm alias để import sẵn có (CHAT_THEME) không vỡ. Giá trị KHÔNG đổi.

import { CHAT_THEME } from '../../../theme';
import type { ModuleTheme } from '../../../theme/tokens';

export { CHAT_THEME };
export type { ModuleTheme };
