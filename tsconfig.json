{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "types": ["jest"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "//": "MobileCore = bản vendored của gói ngoài @magiclamp/mobile-core (repo + CI riêng); src/ KHÔNG import nó. Test của nó dùng node/jest global khác cấu hình → loại khỏi typecheck SuperApp.",
  "exclude": ["**/node_modules", "**/Pods", "MobileCore"]
}
