{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "types": ["jest"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "//": "MobileCore = cây vendored có repo + CI riêng (@magiclamp/mobile-core), 0 importer trong src/. Loại khỏi tsc của SuperApp để test nội bộ của nó không tạo lỗi ma ở đây.",
  "exclude": ["**/node_modules", "**/Pods", "MobileCore"]
}
